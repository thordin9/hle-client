#!/usr/bin/make -f
export DH_VERBOSE = 1

%:
	dh $@

override_dh_auto_configure:
	:

override_dh_auto_build:
	:

override_dh_auto_test:
	:

override_dh_auto_install:
	# dh_installinit handles debian/hle-client.default → /etc/default/hle-client
	# Install the systemd unit manually so dh_installsystemd (overridden below)
	# does not auto-enable or auto-start the service on installation.
	install -D -m 644 debian/hle-client.service \
		debian/hle-client/lib/systemd/system/hle-client.service

override_dh_installsystemd:
	:
